notion_key = "notion_key"
database = "database"

gemini_key="gemini_key"
webster_key = "webster_key"

# Optional: Anki deck new cards are pushed into (push_to_anki.py).
# Defaults to "Word Bank" if not set. See issue #9.
anki_deck = "Word Bank"
